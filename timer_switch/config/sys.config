%% Application environment

[{kernel,
  [{logger_level, all},
   {logger,
    [{handler, log_to_file, logger_std_h,
      #{level => all,
        config => #{file => "timer_switch.log"}}}
    ]}
  ]},
 {timer_switch, []}
].
